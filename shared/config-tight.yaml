# Smallest terminal we are willing to present at: a projector with the font
# turned well up. If a slide fits here, it fits anywhere.
export:
  dimensions:
    columns: 90
    rows: 26
snippet:
  exec:
    enable: true
